{
  "name": "edituno",
  "version": "2.2.6",
  "private": true,
  "description": "Free local video editor PWA with mobile-first editing, desktop workspace, SEO-ready metadata and social sharing preview",
  "scripts": {
    "build": "tsc -p tsconfig.json && node tools/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "python -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  }
}
