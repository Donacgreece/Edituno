# Edituno v1.0.1

- Restored the requested Cloud Violet Edituno app icon.
- Same icon now used for favicon, PWA, Apple touch icon and splash screens.
- Splash dismissal no longer depends on successful app initialization.
- Added startup fallback if initialization fails.
- Service worker cache bumped to v1.0.1 and update cache bypass enabled.
- Deployment script no longer uses a `finally` block.
